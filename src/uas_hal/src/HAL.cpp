// Local library incldues
#include <uas_hal/HAL.h>

using namespace uas_hal;

// Constructor
/*
namespace controller
{
	// Default constructor
	Controller::Controller(double dt) : DT(dt) {}

	// Obtain the control
	double Controller::limit(double v, double minval, double maxval)
	{
	    // Clamp a variable to a fixed range
	    if (v < minval)
	        v = minval;
	    if (v > maxval)
	        v = maxval;
	    return v;
	}
}
*/